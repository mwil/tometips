<div class="accordion navigation-accordion ui-nav-races-accordion" id="races-navigation">
    {{#each race_list}}
        <div class="accordion-item">
            <h2 class="accordion-header" id="heading-{{toHtmlId short_name}}">
                <button class="accordion-button collapsed" type="button" 
                        data-bs-toggle="collapse" data-bs-target="#nav-{{toHtmlId short_name}}" 
                        aria-expanded="false" aria-controls="nav-{{toHtmlId short_name}}">
                    {{toTitleCase display_name}}
                </button>
            </h2>
            <div id="nav-{{toHtmlId short_name}}" class="accordion-collapse collapse" 
                 aria-labelledby="heading-{{toHtmlId short_name}}" data-bs-parent="#races-navigation">
                <div class="accordion-body p-0">
                    <div class="list-group list-group-flush">
                        {{#each subrace_list}}
                            <a href="#races/{{toHtmlId ../short_name}}/{{toHtmlId short_name}}{{currentQuery}}" 
                               class="list-group-item list-group-item-action border-0">
                                {{toTitleCase display_name}}
                            </a>
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>
    {{/each}}
</div>
